import React from "react";

export const metadata = {
  title: "Notes - Notegraph AI",
  description: "Notes - Notegraph AI",
};

export default function NotesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
