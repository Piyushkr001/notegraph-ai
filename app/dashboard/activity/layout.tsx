import React from "react";

export const metadata = {
  title: "Activity - Notegraph AI",
  description: "Activity - Notegraph AI",
};

export default function ActivityLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
